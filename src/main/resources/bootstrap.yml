spring:   
  cloud:
    config:
      uri: http://localhost:8887
      
  profiles:
    active: default

#spring.application.name=eurekaExample
#
#cloud.config.uri=${CONFIG_SERVER_URL:http://localhost:8887}